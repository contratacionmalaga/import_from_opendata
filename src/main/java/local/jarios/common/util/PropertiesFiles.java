package local.jarios.common.util;

/**
 * Clase para la gestión de los ficheros properties.
 *
 * @author Home
 */
public final class PropertiesFiles {

  /** Nombre sin extensión del fichero app.properties. */
  public static final String APP = "app";

  /** Nombre sin extensión del fichero filter.properties. */
  public static final String FILTER = "filter";

  /** Nombre sin extensión del fichero hibernate.properties. */
  public static final String HIBERNATE = "hibernate";

  /** Nombre sin extensión del fichero jakarta_filtro.properties. */
  public static final String JAKARTA_FILTRO = "jakarta_filtro";

  /** Nombre sin extensión del fichero jakarta_principal.properties. */
  public static final String JAKARTA_PRINCIPAL = "jakarta_principal";

  /** Nombre sin extensión del fichero mail.properties. */
  public static final String MAIL = "mail";

  /** Constructor privado para evitar la instanciación. */
  private PropertiesFiles() {
    // Evita la instanciación
  }
}
