# Release 7.3.1

Fecha: 2026-09-09
Rama: `jarp/fix-release-package-artifacts-v7.3.1`

## Cambios

- Se corrige el workflow `Release Package` para generar los cuatro ejecutables operativos activando explicitamente los perfiles Maven de release.
- Se deja de publicar como asset cualquier JAR generico generado sin perfil, evitando nombres con propiedades Maven sin resolver como `artifact.suffix.-7.3.0-.profile.suffix.jar`.
- Se valida antes de subir assets que existen exactamente los JAR esperados para `con_filtros` y `sin_filtros` en modos `local` e `internet`.
- Se elimina el `deploy` generico sin perfil del workflow de release para evitar paquetes Maven ambiguos con coordenadas no representativas de los ejecutables operativos.
- Se actualiza la version Maven del aplicativo a `7.3.1`.

## Artefactos esperados

```text
opendata_con_filtros-7.3.1-local.jar
opendata_con_filtros-7.3.1-internet.jar
opendata_sin_filtros-7.3.1-local.jar
opendata_sin_filtros-7.3.1-internet.jar
```

## Validacion

```powershell
.\mvnw.cmd test
.\mvnw.cmd spotless:check
.\mvnw.cmd clean package -Pcon-filtros-local -DskipTests
.\mvnw.cmd package -Pcon-filtros-internet -DskipTests
.\mvnw.cmd package -Psin-filtros-local -DskipTests
.\mvnw.cmd package -Psin-filtros-internet -DskipTests
```

Resultado local: correcto. Se generan los cuatro JAR ejecutables esperados para `v7.3.1` y no aparecen nombres con `artifact.suffix` ni `profile.suffix` sin resolver.

Pendiente de validar en GitHub Actions antes de publicar `v7.3.1`.
